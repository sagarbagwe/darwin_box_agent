# Darwinbox HR Agent Deployment

This project deploys a Vertex AI Agent using a single deployment script and a `.env` file for configuration.

## Setup Instructions

### Step 1: Create a Python Environment

It's recommended to use a virtual environment:

```bash
python3 -m venv venv
source venv/bin/activate